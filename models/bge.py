
# from sentence_transformers import SentenceTransformer

# def embed_model(model, documents):
#     model = SentenceTransformer(model)
#     embeddings = model.encode(documents, convert_to_numpy=True)
    
#     return embeddings 

